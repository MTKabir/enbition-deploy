apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth-service
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
    # - name: metrics
    #   protocol: TCP
    #   port: 9090
    #   targetPort: 9090
